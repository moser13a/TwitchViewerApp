---
kind: pipeline
type: docker
name: default
steps:
  - name: backend
    image: node
    commands:
      - npm install
      - node index.js
trigger:
  branch:
    - master
  event:
    - push
---
kind: pipeline
type: docker
name: backend
steps:
  - name: backend
    image: node
    commands:
      - echo "tsting"
depends_on: default
